/* eslint-disable no-unused-vars */
export enum payload {
  CLOSE   = "close",
  CLOSED  = "closed",
  CLOSING = "closing",
  OFF     = "off",
  OPEN    = "open",
  OPENING = "opening",
  STOP    = "stop",
  STOPPED = "stopped",
}